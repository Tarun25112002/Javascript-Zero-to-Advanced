let names = ["Tarun", "Tanvi", "Sunita", "Dhirendra"]
// 1) Extract normal strings (parents contains strings)
// const [myName, sisName, ...parents] = names;

// 2) Convert each element into an array, so destructuring assigns arrays
// const [myName, sisName, ...parents] = names.slice(0).map(n => [n]);

// console.log(myName);
